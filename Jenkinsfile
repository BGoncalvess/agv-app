pipeline {
    agent any
    stages {
        stage('Connect to GitHub') {
            steps {
                skipDefaultCheckout(true)
                echo 'Connecting to GitHub...'
            }
        }
    }
}
